# auto-generated by grc.converter

id: gnmax2769_source_cc
label: MAX2769 Source
category: Sources
flags: throttle

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: real
    default: samp_rate
-   id: bias
    label: Bias Voltage
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: ['Off', 'On']
-   id: ant
    label: Antenna Select
    dtype: int
    default: '3'
    options: ['3', '1', '2', '0']
    option_labels: [Auto, Ant 1, Ant 2, 'Off']
-   id: freq
    label: Frequency (Hz)
    dtype: real
    default: '1575420e3'
-   id: bw
    label: Bandwidth
    dtype: int
    default: '0'
    options: ['0', '1', '2', '3']
    option_labels: [2.5MHz, 4.2MHz, 8MHz, 18MHz]
-   id: zeroif
    label: Zero IF
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: ['Off', 'On']

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnMAX2769
    make: gnMAX2769.gnmax_source_cc(${bias}, ${ant}, ${freq}, ${bw}, ${zeroif})
    callbacks:
    - set_bias(${bias})
    - set_ant(${ant})
    - set_freq(${freq})
    - set_bw(${bw})

file_format: 1
